<%= form_for :event do |f| %>
      <div class="field">
				<%= e.label :date %><br />
				<%= e.text_field :date %>
			</div>
			<div class="field">
				<%= e.label :type %><br />
				<%= e.text_field :type %>
			</div>
			<div class="field">
				<%= e.label :invoice_number %><br />
				<%= e.text_field :invoice_number %>
			</div>
    <% f.fields_for :product_count do |pc| %>  
      <p>  
      test
      </p>  
    <% end %> 
<% end %>
