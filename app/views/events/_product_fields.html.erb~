<% if product.display? %>
<div class="product">
	<h3><%= product.name %></h3>
	<% if product.imloc %>
		<%= image_tag product.imloc %>
	<% else %>
		<img src="http://terryshoemaker.files.wordpress.com/2013/03/placeholder1.jpg">
	<% end %>
	<%= f.hidden_field :product_id, value: product.id %>
	<%= f.hidden_field :is_box, value: "false" %>
	<%= f.hidden_field :count, value: 0 %>
	<center><table class="table-condensed">
	<tr>
	<td style="text-align:center">Box Count</td>
	<td style="text-align:center">Loose Piece Count</td>
	</tr>
	<tr>
	<td style="text-align:center"><%= f.text_field :box_count, value: nil, 	class: "input-small", placeholder: "Box Count"  %></td>
	<td style="text-align:center"><%= f.text_field :piece_count, value: nil, 	class: "input-small", placeholder: "piece Count"  %></td>
	</tr>
	</table></center>
				<% if product.description %>
					<%= product.description %> - 
				<% end %>
				<% if product.per_box %>
				<%= product.per_box %>
				<% else %>
				1
				<% end %>
				 pc/box
</div>
<% end %>
