<% if product.display? %>
<div class="product">
	<h3><%= product.name %></h3>
	<%= f.hidden_field :product_id, value: product.id %>
	<%= f.hidden_field :is_box, value: "false" %>
	<%= f.text_field :box_count, value: nil, 	class: "input-small", placeholder: "Box Count"  %>
	<%= f.text_field :peice_count, value: nil, 	class: "input-small", placeholder: "Peice Count"  %>
	<br>
	<table>
		<tr>
			<td>
				<% if product.imloc %>
					<%= image_tag product.imloc %>
				<% end %>
			</td>
			<td>
				<% if product.description %>
					<%= product.description %> - 
				<% end %>
				<%= "#{product.per_box} pc/box" %>
			</td>
		</tr>
	</table>
</div>
<% end %>
