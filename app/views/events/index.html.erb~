<h1><%= @events.first.event_type %></h1>
<table>
<tr>
<td>
Date
</td>
<% if @events.first.event_type == "Product Order" %>
<td>
	Invoice #
</td>
<% end %>
<% Product.all.each do |p| %>
	<td>
	<%= p.name %>
	</td>
<% end %>
</tr>
<% @events.each do |event| %>
	<tr>
		<td><%= event.date %></td>
			<% if event.event_type == "Product Order" %>
				<td><%= event.invoice %></td>
			<% end %>
		<% event.product_counts.each do |pc| %>
			<td><%= pc.count %>  
			<% if pc.is_box %>
				boxes
			<% else %>
				pieces
			<% end %>
			</td>
		<% end %>
	</tr>
<% end %>
</table>

