<div style="float:left">
<%= link_to("< Previous", product_path(product.previous), class: "btn") if !product.previous.nil? %>
</div>
<div style="float:right">
<%= link_to("Next >", product_path(product.next), class: "btn") if !product.next.nil? %>
</div>
